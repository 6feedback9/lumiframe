{
  "name": "@lumiframe/storage",
  "version": "0.1.0",
  "private": true,
  "description": "Object storage wrapper: private buckets, signed URLs. LocalFsStorageAdapter (no external deps) for dev/CI, SupabaseStorageAdapter for real deployments — picked automatically from env, same as MockTryOnProvider vs a real AI vendor.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
