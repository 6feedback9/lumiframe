{
  "name": "@lumiframe/sdk",
  "version": "0.1.0",
  "private": true,
  "description": "Universal JS SDK merchants embed on their product pages. See README.md for the API contract.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsup src/index.ts --format iife,esm --minify --dts --global-name TryOn",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@lumiframe/widget": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.2.4",
    "typescript": "^5.5.4"
  }
}
