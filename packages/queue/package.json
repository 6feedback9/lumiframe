{
  "name": "@lumiframe/queue",
  "version": "0.1.0",
  "private": true,
  "description": "TryOnQueue abstraction. InMemoryTryOnQueue (no Redis) for dev/CI, BullMqTryOnQueue for real deployments — picked from env, same pattern as storage and providers.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "bullmq": "^5.12.9"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
