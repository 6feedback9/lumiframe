# packages/integrations/shopify — Phase 3

Shopify app (prefer the current extension/app architecture over manual
theme edits): OAuth, product detection via Shopify's runtime, Try-On button
injection, `OrderTrackingAdapter` implementation (cart/order webhooks),
order attribution. See product spec §12 and `ARCHITECTURE.md` §10.

Not yet implemented — this is a placeholder for Phase 3.
