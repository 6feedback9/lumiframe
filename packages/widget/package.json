{
  "name": "@lumiframe/widget",
  "version": "0.1.0",
  "private": true,
  "description": "The try-on widget UI, lazy-loaded by @lumiframe/sdk on open(). Talks to the async /api/v1/tryons flow (ARCHITECTURE.md §7); visual language ported from the LumiOn MVP's widget.js.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.4"
  }
}
